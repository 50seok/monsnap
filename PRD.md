# PRD — MonSnap: 인물 사진 → 몬스터 캐릭터 생성 앱

> **문서 버전:** v0.3 · **작성:** 2026-08-08 · **수정:** 2026-08-10 (검수 반영 · Streamlit 전환 · 시드 정책 · $0 절감 경로 · IP 가드레일 보강) · **상태:** 초안 (구현 착수 전)
> 프로젝트명 "MonSnap"(monster + snap)은 가칭 — IP 리스크 회피를 위해 "포켓몬" 명칭을 배제한 이름이며 변경 가능.

---

## 1. 개요

카메라로 인물 사진을 찍으면, 그 사람의 특징(헤어스타일·안경·얼굴형·표정)을 캐리커처처럼 반영한 **오리지널 몬스터 캐릭터 이미지**를 생성해 주는 앱.

| 항목 | 내용 |
|---|---|
| 목적 | 포트폴리오·AI 학습용 → 추후 실서비스 염두 |
| 형태 | **Streamlit 프로토타입** 먼저 → 실서비스 결정 시 React 웹(PWA)/Expo 앱으로 전환 |
| 생성 전략 | **하이브리드**: 상용 API로 MVP → 직접 LoRA 파인튜닝으로 교체 |
| 보유 자원 | **노트북 1대** — Ryzen 7 7735HS, RAM 16GB, SSD 477GB, **RTX 4060 Laptop GPU(VRAM 8GB)** + 내장 Radeon 듀얼 구성. OCI 무료 계정(예정) |

## 2. 배경 · 문제 정의

- "내 사진으로 나만의 몬스터를 만든다"는 확실한 재미 요소가 있는 생성형 AI 응용.
- 단순 API 호출 데모가 아니라 **직접 모델을 학습(파인튜닝)한 경험**을 포트폴리오로 증빙하는 것이 차별점.
- 파이프라인(확정): ① 포켓몬 스타일 학습 → ② 인물 촬영 → ③ 특징 추출(캐리커처) → ④ 특징 반영 몬스터 생성 — **생성은 마지막에 한 번만** 일어난다.
- 원 기획 1.2의 "새 포켓몬 이미지 생성"은 서비스 단계가 아니라 **학습 검증** 용도: LoRA 학습 직후 샘플을 생성해 스타일 습득 여부를 확인하는 1회성 절차.

## 3. 사용자 시나리오 (MVP)

**정상 경로**
1. 웹앱 접속 → "사진 찍기/선택" (모바일: 카메라 또는 앨범)
2. 원본 미리보기 확인 → "몬스터 생성" 버튼
3. 대기(진행 표시) → 원본·결과 나란히 표시
4. 결과 저장(PNG 다운로드) / **다시 생성**(= 같은 원본으로 재생성, 매번 다른 결과) / 새 사진 선택

**실패 경로** (모든 실패는 사용자에게 원인 + 다음 행동을 안내)
- 파일 문제(이미지 아님·10MB 초과) → 즉시 오류 메시지 + 재선택 유도
- 생성 API 오류·타임아웃 → "잠시 후 다시 시도" 메시지 + 재시도 버튼
- **안전 필터 차단**(정책상 거부 — 아동 사진 등에서 발생 가능) → "이 사진은 생성할 수 없습니다. 다른 사진을 선택해 주세요" + 사진 재선택 유도

## 4. 기능 요구사항

| ID | 요구사항 | Phase | 우선순위 |
|---|---|---|---|
| FR-1 | 사진 촬영·업로드 (모바일 카메라 + 갤러리) — 이미지 형식·10MB 검증, 위반 시 오류 메시지 | 1 | P0 |
| FR-2 | 특징 반영 몬스터 이미지 생성 | 1 | P0 |
| FR-3 | 결과 표시·PNG 저장·재생성 | 1 | P0 |
| FR-4 | 오류 처리: API 실패·타임아웃·안전필터 차단을 §3 실패 경로대로 안내 | 1 | P0 |
| FR-5 | 자체 학습 모델로 생성 교체 (FLUX LoRA + PuLID) | 2 | P1 |
| FR-6 | API 결과 vs 자체 모델 비교 갤러리 (포트폴리오 증빙) | 2 | P2 |
| FR-7 | 모바일 앱 패키징 (Expo), 스토어 배포 | 3 | P2 |

## 5. 비기능 요구사항

- **보안**: 생성 API 키는 백엔드에만 보관(프론트 번들 노출 금지).
- **개인정보**:
  - 업로드 사진은 서버에 저장하지 않음(생성 후 폐기).
  - 단, **생성을 위해 사진이 외부 API(구글 Gemini)로 전송됨** — "저장 안 함"과 별개의 처리이므로, 배포 시 첫 화면에 이 사실을 고지하고 **개인정보처리방침·이용약관을 준비**한다(§10).
- **성능·타임아웃**: 생성 대기 최대 2분 허용, 진행 상태 표시 필수(`st.spinner`). Streamlit은 단일 프로세스 동기 처리라 별도 타임아웃 설계 불필요 — API 호출에만 150초 타임아웃, 초과 시 §3 실패 경로. (실서비스 전환 시엔 VM 프록시 + 서버리스 타임아웃 회피 설계 필요)
- **악용 방지**: 프로토타입 단계는 **URL 비공개 공유**(지인 테스트 한정)로 갈음 — Streamlit Cloud에는 IP 기반 제한을 붙이기 어려움. 실서비스 전환 시 rate limit(IP당 시간당 10회) 적용.
- **비용 상한**: 학습(1회성) 총 $30 이하, API 호출(경상) 월 $10 이하 목표. §9와 정합.

## 6. 기술 선정 (2026-08-08 웹 리서치 4건 기반)

### 6.1 포켓몬 스타일 "학습" 도구 (원 질문 2.1)

| 용도 | 선정 | 근거 |
|---|---|---|
| LoRA 학습 1순위 | **fal.ai LoRA trainer** (FLUX.1-dev) | 업로드만으로 25분~1시간, **$2~10/회**, GPU 불필요 |
| LoRA 학습 대안 | kohya_ss + RunPod RTX4090 | $4~9/회, 하이퍼파라미터 직접 제어(난이도↑) |
| 학습 연습(무료) | **노트북 로컬 SDXL LoRA** — kohya 저사양 설정(batch 1, gradient checkpointing, 8bit 옵티마이저) | 8GB에서 가능, 수 시간 소요. 전원 연결·발열 관리 필수 |
| 데이터셋 1안 | HuggingFace `lambdalabs/pokemon-blip-captions` (833장, 캡션 완비) | 전처리 없이 바로 학습 투입. **사용 전 라이선스 표기 확인** |
| 데이터셋 2안 | **PokéAPI official-artwork** (1,000+장, 공식 일러스트라 스타일 균일) | 캡션 자동 생성 필요(BLIP/VLM 스크립트). 팬 운영 API — 저작권은 어느 소스든 닌텐도 소유 |
| 비교 기준선 | CivitAI 공개 포켓몬 LoRA (5,900+개 존재) | 내 학습 결과 품질 비교용 (학습 재료 아님) |

⚠️ **RTX 4060 Laptop(8GB)은 FLUX LoRA 학습에는 부족**(권장 16GB). 로컬은 **SDXL 학습 연습 + 추론 실험용**(FLUX는 fp8/GGUF 양자화 추론만), 최종 FLUX LoRA는 클라우드로.
⚠️ **OCI 무료 티어는 GPU 없음**(ARM VM) → 학습용이 아니라 **백엔드 무료 호스팅**용.

### 6.2 인물 특징 추출 도구 (원 질문 2.2)

| 단계 | 선정 | 비고 |
|---|---|---|
| MVP (Phase 1) | **Gemini 이미지 API (Nano Banana 계열)** img2img | 특징 추출+스타일 변환을 API 한 번에. ~$0.045/장 (무료 할당량은 키 발급 후 실측 — §12) |
| 자체 모델 (Phase 2) | **PuLID(정체성 어댑터) + 자체 스타일 LoRA** | 2026년 기준 정체성 보존 최우수. 호스팅 방식은 §12 오픈 이슈 |
| 폴백 | SDXL + IP-Adapter FaceID | PuLID가 로컬 8GB에서 무거울 때 |

**배제**: 비전 LLM으로 특징을 텍스트화해 프롬프트에 넣는 방식(특징 일반화로 정체성 손실 큼), MediaPipe/face-api.js 랜드마크(텍스처·외형 미포착).

## 7. 아키텍처 (Phase 1)

```
[브라우저] ← Streamlit UI
   ├─ st.camera_input (촬영) / st.file_uploader (앨범 — 모바일에선 카메라 시트도 뜸)
   ↓ 같은 파이썬 프로세스 (app.py 파일 1개)
[이미지 검증 → Gemini 이미지 API 호출 → 결과 표시·PNG 다운로드]
        키는 st.secrets — 브라우저 노출 없음, 별도 프록시 서버 불필요
```

- 호스팅: **Streamlit Community Cloud** 무료 — GitHub 연동 자동 배포, `st.secrets`로 키 관리. 방치 시 슬립했다가 방문 시 깨어남(포트폴리오 데모로 충분).
- 프론트+백엔드 분리가 없어 React+FastAPI 구성 대비 코드량 ~1/5, CORS·프록시·OCI 설정 전부 불필요.
- 모바일 UX: `st.camera_input`은 웹캠 방식이라 어색할 수 있음 → `st.file_uploader` 병행 제공(모바일에서 카메라/앨범 선택 시트).
- **최소 로깅**: 요청 시각·성공/실패·API 응답시간을 stdout에 남긴다(성공 지표 검증·오류 추적용). 사진·개인정보는 로그에 기록하지 않음.
- Phase 2 교체 지점: app.py의 생성 함수 1곳만 fal.ai(FLUX LoRA + PuLID) 호출로 교체. 비교 갤러리(FR-6)도 Streamlit으로 구현.
- **실서비스 전환 시(Phase 3)**: React(Vite)+FastAPI 프록시(OCI VM)+Cloudflare Pages 구성으로 재구축 — 준비된 스캐폴드 재활용. OCI ARM 주의사항(용량 확보·aarch64 휠 호환)은 그 시점에 적용.

## 8. 로드맵

| Phase | 내용 | 기간 | 산출물 |
|---|---|---|---|
| **1. 프로토타입** | Streamlit `app.py` — API 기반 e2e (촬영→생성→저장), Streamlit Cloud 배포 | 3일~1주 | 동작하는 데모 URL |
| **2. 직접 학습** | ① 데이터셋 준비·캡션(2~3일) → ② fal.ai LoRA 학습·검증(2~3회 반복) → ③ PuLID 조합 추론 파이프라인 → ④ 서비스 연결 → ⑤ 비교 갤러리 | 3~6주 (PuLID·ComfyUI 첫 도입 기준) | 자체 모델 + 비교 갤러리 + **README/블로그 케이스 스터디**(기술 선택 근거·학습 과정 기록) |
| **3. 실서비스 전환** | React 웹(PWA) 재구축(스캐폴드 재활용) + Expo 패키징, 스토어 배포, 오리지널 브랜딩 확정 | 추후 | 스토어 등록 앱 |

## 9. 비용 계획

| 항목 | 비용 | 성격 |
|---|---|---|
| Phase 1 API | ~$0.045/장 × 테스트 200~300장 = **$9~14** (무료 할당량 확인 시 그만큼 차감 — §12) | 경상(월) |
| Phase 2 학습 | $5~30 (fal.ai LoRA 2~3회 + 추론 테스트) | **1회성** |
| Phase 3 스토어 | 첫해 $124 (Apple $99/년 + Google Play $25 일회성) | 1회성/연간 |
| 호스팅 | $0 (Phase 1: Streamlit Community Cloud / Phase 3: Cloudflare Pages + OCI 무료 VM) | — |
| 도메인 | $0 (무료 서브도메인) — 커스텀 도메인 원할 시 $10~15/년 선택 | 선택 |

**$0 절감 경로 (지출 최소화 원칙)**
- Phase 1: Gemini **무료 티어**(일일 한도, 키 발급 후 실측 — §12-3) 내에서 개발·테스트 진행 + 코드 작성 전 컨셉 검증은 **AI Studio 웹 UI**(무료)로 수행 → 사실상 $0.
- Phase 2: 학습을 노트북 SDXL LoRA(무료)로 수행하는 대안 있음 — 품질 타협(FLUX 대비 한 단계 아래). 최종 고품질 FLUX LoRA 1회($2~10)만 선택 지출.
- 회피 불가 지출은 Phase 3 스토어 등록($124)뿐 — 실제 출시 결정 시에만 발생.

## 10. 법적 리스크 · 가드레일

**IP (포켓몬)**
- 포켓몬은 닌텐도/포켓몬컴퍼니 IP. 팬 프로젝트 테이크다운 이력 다수, "포켓몬" 명칭 사용 앱은 Apple 심사 거부율 70~80% 수준(리서치 기준).
- 상용 API들도 정책상 캐논 캐릭터(피카츄 등) 생성을 차단 → 프롬프트는 "cute original monster creature" 계열만 사용.
- **포트폴리오 단계**: 리스크 낮음. 학습용 포켓몬 데이터셋 사용은 가능하되 README에 비상업·연구 목적 + 데이터셋 출처·라이선스 표기.
- **실서비스 단계(필수)**: 앱명·설명에서 "포켓몬/Pokémon/포케" 완전 배제, 몬스터볼 등 상징 UI 금지, 기존 포켓몬 이미지 재사용 금지.
- **실서비스 시 모델 클린화(필수)**: 서비스용 모델은 포켓몬 데이터셋이 아닌 **클린 데이터**(오리지널 커미션 아트·라이선스 확인된 크리처 아트)로 **재학습** — Phase 2 학습 파이프라인 그대로 재사용, 포켓몬 학습본은 비공개 연습·포트폴리오 증빙까지만. (근거: 학습 단계 복제는 해외 판결상 허용 기조나 한국은 TDM 면책 미입법 — 상업화 시 회색지대 회피)
- **출력 가드**: 특정 포켓몬과 실질적 유사한 생성물이 침해의 실체(실제 패소 사례 전부 이 유형) → 프롬프트에 특정 캐릭터 모방 금지 명시 + 출시 전 생성 샘플 육안 스크리닝.
- 참고 실증: 명칭·캐릭터를 배제한 몬스터 수집 장르(Temtem·Coromon 등)는 수년째 무사고 운영, 팰월드도 저작권으로는 공격받지 않음(특허 공격은 MonSnap과 무관한 게임 메커니즘 대상). 단 본 문서는 법률 자문이 아니며, 실제 출시 전 전문가 검토 권장.

**개인정보 (얼굴 사진)**
- 얼굴 사진은 개인정보 — 공개 배포 전 **개인정보처리방침 + 이용약관 페이지 준비**: 수집 항목(사진), 목적(생성), **제3자 전송(구글 Gemini) 고지**, 보관(생성 후 즉시 폐기, 서버 미저장), 문의처.
- **실서비스 단계**: 14세 미만 이용 제한(또는 법정대리인 동의) 조항 포함 — 개인정보보호법 요건.

**생성물**
- AI 단독 생성물은 저작권 보호를 못 받는다는 판단이 누적(미국 저작권청 등) → 이용약관에 "생성 이미지는 자유 사용 가능, AI 생성물임을 고지 권장"으로 명시해 분쟁 소지 제거.

## 11. 성공 지표

- **Phase 1**: 모바일 실기기에서 촬영→생성→저장 e2e 성공(프록시 로그로 확인).
  - 닮음 평가: 지인 5명이 각자 사진으로 생성 후 4지선다 응답 — (a) 많이 닮음 (b) 특징 일부 알아봄 (c) 스타일만 비슷 (d) 전혀 다름. **a+b가 3/5 이상이면 통과**, 미달 시 프롬프트 조정 후 1회 재시도.
- **Phase 2**: 동일 인물 10장 기준 [API vs 자체 LoRA] 비교표 작성(정체성 보존·스타일 일관성 채점) — 포트폴리오 증빙 자료 완성.

## 12. 오픈 이슈 (결정 대기)

| # | 이슈 | 결정 시점 |
|---|---|---|
| 1 | 프로젝트명 확정 (현재 가칭 MonSnap) | Phase 1 착수 시 |
| 2 | 구현 착수 여부·시점 — Phase 1은 Streamlit `app.py` 신규 작성 (기존 React+FastAPI 스캐폴드는 Phase 3용으로 보류) | 사용자 판단 |
| 3 | Gemini 모델 버전 + **무료 할당량** 확정 (기본 `gemini-2.5-flash-image`, 키 발급 후 실측) | Phase 1 첫 호출 시 |
| 4 | **결과물 스타일 방향성**: 마스코트형(사람 얼굴이 보임) ↔ 완전 크리처형(특징을 은유로만 반영) | Phase 1 샘플 생성 후 |
| 5 | Phase 2 추론 호스팅 방식: ComfyUI 자가 구성(무료, 셋업 복잡) vs fal.ai 커스텀 LoRA 엔드포인트(유료, 간편) | Phase 2 착수 시 |
| 6 | 데이터셋 최종 선정: lambdalabs 833장 vs PokéAPI 공식 아트 1,000+장(캡션 자동 생성) | Phase 2 착수 시 |
| 7 | Phase 3 브랜딩(오리지널 몬스터 세계관) | Phase 2 완료 후 |
| 8 | **시드 정책** — 유력안 = **계층 시드**: 얼굴 임베딩(주 키)이 종족·실루엣 결정, 이름 해시(보조)는 색·장식만 결정 + 속성 선택(불/물/풀) 옵션. Phase 1은 이름 제약 없음(이름 바꿔 다중 생성 = 놀이로 수용, 비용은 횟수 제한으로 방어), 계층 시드는 Phase 2~실서비스에서 적용 | Phase 1 샘플 생성 후 |

## 13. 구현 상태 (2026-08-14) — 중단 시점 인계

### 진행 상황 (2026-08-14 저녁 — SDXL 전환 완료, e2e PASS)
- **1단계 프로브 통과**: Animagine XL 4.0(danbooru 학습 — `pokemon (creature)` 태그 네이티브)
  + `ip-adapter-faceid_sdxl.bin` @0.2 + img2img 0.85로 현 파이프라인 재현.
  SD1.5 대비 입 선명도·팔다리 분리·특징(안경) 가시성 전부 우세(사진 5장 실측).
- **2단계 cutemon SDXL LoRA 학습 완료**: `training/train_text_to_image_lora_sdxl.py`
  (diffusers v0.39 예제 + **8GB 대응 수정: 동결 TE 2개 CPU 이전** — 미수정 시 OOM 실측,
  gitignore라 재현 시 같은 수정 필요). Animagine 베이스 · fp16-fix VAE · rank16 · 512px ·
  5000스텝(3h43m, $0) → 스윕 실측으로 **checkpoint-4000 채택**(v3 때와 동일 패턴,
  5000은 낙서성 아티팩트) → `streamlit_app/models/style_lora_sdxl/` 배포(gitignore).
  CFG는 6보다 **9**가 입·특징 견인 우세(타이브레이커 실측).
- **앱 SDXL 전환 완료**(`app.py`): SDXL img2img + faceid_sdxl + cutemon-XL LoRA 1.0,
  생성 1024px(GEN_SIZE) · 크롭/원형 인덱스는 512 유지. v2 혼합·attn-proc 변환 제거.
  프롬프트 = 학습 캡션 어형 선두 + danbooru 태그, **77토큰 이내 재설계**(구 버전은 94토큰
  으로 꼬리 유실 실측). e2e PASS(_01·_03, 도감 카드 포함).
- **남은 품질 이슈**: ① _03류 얼굴 붕괴 — 안경 특징 × 팔 든 원형(삐삐) 조합에서 눈·안경
  뭉개짐(빈 눈 네거티브로 불충분) ② 팔레트 견인 약함 — 부분 적용(안경 브리지·귀 끝 색)
  까지는 확인, 몸 전체 색 전환은 미달. 다음 세션 과제.
- 실행 함정: 이 노트북에서 SD 프로세스를 **백그라운드 래퍼로 띄우면 첫 생성에서 조용히
  죽는 사례 3회**(원인 미상, RAM·크래시 로그 무관) — 프로브·스모크는 포그라운드로 돌릴 것.

### 진행 상황 2 (2026-08-15 새벽) — 오리지널 원형 도감(proto_dex) 전환
- **문제**(사용자 지적): 포켓몬 원형 앵커라 결과가 특정 포켓몬과 과닮음 + IP 리스크.
  통찰 "포켓몬도 실제 동물 모티브" → **모티브 원천(동물)에서 직접 출발**하기로.
- **1차 시도(동물 사진 직접 앵커)**: 배경·자세 통제 안 되면 덩어리(곰 실측) →
  **2단계 방식 채택**: 동물 사진 → 오프라인 배치로 크리처 원형 사전 생성(체형별 부위
  어휘 명시) → 부위 가독성 기준 큐레이션 → 그 원형을 앵커 풀로.
- **구축 완료**: 위키미디어 37종 수집(`dataset/animals/`, rembg 전처리) × 4시드 =
  148 후보(`dataset/proto_dex/_candidates/`) → **30종 채택 = `dataset/proto_dex/`**
  (제외: 원본 사진 불량 3 + 부위 안 읽히는 블롭형 4). 풀 전환은 secrets `REF_DIR`
  (기본값 코드 = dataset/cute 롤백용). 체형 필터는 heads/ball/blob만 배제로 완화
  (큐레이션이 필터 대체 — cute 풀 롤백 시 구 필터 복원 권장, app.py 주석 참조).
- **스모크 실측**: _01→요정날개 크리처, _03→물고기 크리처 — 특정 포켓몬 유사성 소멸,
  부위(날개·꼬리·지느러미·발) 가독, 안경 특징·속성색(아쿠아) 반영. 사용자 확인 대기.
- **속성 시그니처 추가**(사용자 피드백 "파이리는 꼬리 불꽃·발톱이 있는데 이건 그냥
  만들어진 무언가"): TYPES에 속성별 시그니처 부위 구절 추가(불=꼬리 불꽃+발톱,
  풀=머리 새싹+잎 꼬리 등) + 네거티브에서 `claws` 제거(발톱 억제 원인이었음).
  실측: 불=발톱, 풀=새싹 또렷 / 전기·페어리는 부분적. 토큰 최악 75/77.
- **GitHub 공개 저장소 생성**: https://github.com/50seok/monsnap (원격 origin/master).
  시크릿·IP 자산(데이터셋·가중치) 미포함 확인 후 푸시. 미커밋 작업분은 로컬에만 있음.
- **남은 과제**: ① 종 다양성 확장(30→100+, 수집 스크립트 재실행으로 증분 가능 —
  scratchpad `build_animal_pool.py`·`proto_dex_batch.py` 참조, 레이트리밋 2.5s 스로틀)
  ② 불량 원본 교체(강아지=꽃조형물·다람쥐 수직자세·사막여우 등) ③ 몸 전체 팔레트
  전환은 여전히 약함 ④ 전기·페어리 시그니처 강화(꼬리 계열이 약함 — 프롬프트 앞쪽
  승격 실험) ⑤ ~~SDXL 전환 + proto_dex + 시그니처 전부 미커밋~~ → 커밋 완료(아래).

### 진행 상황 3 (2026-08-15 저녁) — strength 근본 원인 진단·수정 + 커밋
- **재현**: 지인 5장 재실측 결과 3/5가 동일 계열 원형에 매칭되고, 완성본도 여전히
  "포켓몬 원형에 가까운 애매한 덩어리"라는 사용자 피드백. proto_dex 없이(구 REF_DIR=
  dataset/cute) 생성한 비교 스크린샷(2026-08-14 22:05)이 삐삐(Clefairy)와 실루엣·자세·
  귀 끝 색까지 거의 동일 — "동물 경유해도 결국 포켓몬으로 회귀"가 재확인됨.
  물고기형 원형은 눈이 안 보이고 없는 다리를 억지로 만든 티가 남(사용자 지적).
- **근본 원인 진단**(코드 분석): `dataset/full`(LoRA 학습셋)이 **공식 포켓몬 아트
  1026장**임을 확인(0001.png=이상해씨). `strength=0.85`(구 기본값)에서는 img2img가
  원본 레퍼런스 픽셀 대부분을 지우고 포켓몬으로 학습된 LoRA 분포로 재구성 — proto_dex를
  아무리 동물 기반으로 바꿔도 무력화됨. 옛 0.85 값 자체가 `REF_DIR=dataset/cute`
  (포켓몬 아트) 시절 "원형과 닮으면 IP 리스크"라 일부러 높인 값이라, proto_dex 전환
  후엔 오히려 전환 취지를 정면으로 무력화하고 있었음.
- **그리드 실측**(goldfish.png 강제 레퍼런스, 동일 시드): strength 0.85는 lora_weight를
  1.0→0.7→0.4로 낮춰도 원형 구조 전멸(발톱 4족 생물로 재구성) — **lora_weight는
  원인이 아니었음**. strength 0.5/0.6/0.7(lora_weight 무관)은 전부 원형 실루엣(몸통·
  꼬리지느러미) 생존 + 색·무늬·표정은 창작됨. 0.7 채택, 슬라이더 기본값 0.85→0.7.
  부수 효과: 같은 원형에 매칭된 두 얼굴도 결과물이 확실히 달라짐(원형 세부 질감이
  0.85에서는 뭉개졌던 것) — 종 다양성 확장 전에도 "동일 원형처럼 보임" 문제 완화.
- **물고기형 다리 강제**: `build_prompt`가 체형 무관하게 "full body, standing"을
  강제하던 게 원인. `POSE_PHRASES`로 fish/squiggle→swimming pose, tentacles→floating
  pose 분기 추가 — 눈 실종 문제 해소 확인(다리 자체는 strength 수정으로 별도 완화).
- **커밋**: `3ba4d55`(SDXL+proto_dex+시그니처, 기존 미커밋분) +
  strength/pose 수정 커밋(이 절 작성 직후).
- **다음(순서)**: ① 종 다양성 확장(30→100+) ② 불량 원본 교체 ③ 전기·페어리 시그니처
  강화 ④ 팔레트 견인(우선순위 낮음). strength 0.7 반영 후 재실측 권장(위 순서는
  strength 수정 전 기준이라 우선순위 재검토 여지 있음).

### 진행 상황 4 (2026-08-15 밤) — 원형 도감 +10종 확장(30→40)
- **배치 생성 strength 재검증**: 런타임(0.7)과 반대로 사진→원형 변환 단계는 **0.8이
  최적**(0.6은 사진 질감이 "가시털"처럼 남는 아티팩트 확인) — 방향이 다른 변환이라
  런타임 strength 결론을 그대로 적용하면 안 됨. `proto_dex_batch.py` 수정 불필요.
- **수집**: Wikimedia API 레이트리밋(429)으로 목표 20종 중 17종만 신규 확보(37→54,
  나머지 11종은 추후 재시도 필요 — baby_elephant·meerkat·capybara·hermit_crab·
  garden_snail·dragonfly·caterpillar·squid·baby_goat·swan_cygnet·baby_alpaca).
- **큐레이션**(부위 가독성 기준, 시드 최대 3개까지 비교): 17종 중 **10종 채택**
  (chick_chicken·duckling·european_robin·ladybug·otter_pup·parakeet·mouse·
  flying_squirrel·flamingo_chick·budgerigar) → `dataset/proto_dex/` 30→**40종**.
  **7종 제외**: ferret·guinea_pig(원본은 깨끗한데 생성 결과가 매번 원본 동물과
  무관한 형태로 나옴 — 원인 불명, 향후 재시도 여지) / pufferfish(원본이 안 부푼
  평범한 물고기 사진 — 오매칭) / koi_fish(원본이 사실 게 다리 사진 — 완전 오매칭)
  / guppy_fish(원본에 물고기 2마리 — 결과물도 이중 피사체로 나옴) / penguin_chick·
  sloth(원본 배경에 고깔/나뭇가지가 껴서 rembg가 같이 오려냄 — 배경 오염).
  **교훈**: Wikimedia 검색 자동 수집은 원본 품질 검증(단일 피사체·배경 오염 여부)
  없이 그대로 씀 — 다음 확장 라운드에서 원본 프리뷰 확인 단계 추가 권장.
- `dataset/`는 gitignore 대상이라 이미지·manifest 자체는 커밋 안 됨(로컬 전용).

### 진행 상황 5 (2026-08-15 밤) — 불량 원본 점검 + 시그니처 앞쪽 승격
- **불량 원본 교체 재검토**: PRD에 적혀있던 puppy(실제로는 강아지 모양 화단 조형물)·
  squirrel(수직 매달림 포즈)·fennec_fox(여우 2마리 겹침) + 오늘 발견한 pufferfish·
  koi_fish·guppy_fish·penguin_chick·sloth 총 8종을 라이브 풀(`dataset/proto_dex/`)
  대조 — **전부 라이브 풀에 없음**(원래 큐레이션에서 이미 걸러짐). 실사용에 영향 없는
  항목이라 "교체"가 아니라 "나중에 원본 다시 구하면 풀 추가 가능" 수준으로 하향.
- **전기·페어리 시그니처 앞쪽 승격**: `build_prompt`에서 시그니처 위치를 맨 뒤(플랫
  컬러 직전)에서 팔레트 바로 뒤(프롬프트 두 번째 절)로 이동 — CLIP 앞쪽 토큰이 강하다는
  기존 원리(probe7b·c) 재적용. 토큰 수 변화 없음(재배치만). ⚠ GPU 재생성 검증은
  비용 문제로 생략 — 다음 실사용 시 전기/페어리 결과 육안 확인 필요.
- **원형 도감 11종 재시도 + 확장 완료(40→49)**: 레이트리밋으로 실패했던 11종을
  스로틀 2.5s→6s·429 시 15s 백오프로 재수집 → 전부 성공(65종). 배치 생성(4시드)
  → 큐레이션 9종 채택(baby_alpaca·baby_elephant·baby_goat·capybara·caterpillar·
  garden_snail·meerkat·dragonfly·hermit_crab). **2종 제외**: squid·swan_cygnet —
  매니페스트 체형 태그(tentacles·wings)와 실제 생성 결과가 안 맞음(촉수·날개가
  안 보임) — 태그·결과 불일치는 SHAPE_PHRASES 오분기로 이어져 예전 "덩어리" 문제
  재발 우려라 제외. **최종 원형 도감 = 49종**(당초 목표 100종에는 미달, 세션 종료
  전 마지막 확장 라운드).

### 진행 상황 6 (2026-08-15 밤) — 전신샷 얼굴 검출 실패 수정
- **사용자 지적**: 여성 사용자는 정면 얼굴 사진보다 이미 찍어둔 전신·인물샷을
  올리는 경우가 많을 것 — 촬영 가이드 문구보다 실사용 패턴에 맞춰야 함.
- **실측**(합성 테스트 이미지, GPU 불필요): 얼굴이 프레임 높이의 12.5%인 전신샷
  시뮬레이션에서 `DETECT_MAX=640`(구 기본값)은 얼굴 검출 **실패**(중앙 정사각
  폴백 크롭으로 넘어가 몸통이 잘릴 위험) → `DETECT_MAX=1280`은 같은 이미지에서
  **성공**. 속도 비용은 무시 가능(+20ms 내외, 640→1280 실측). 7.3% 극단치는
  2000px까지 올려도 실패해 이건 대응 범위 밖으로 판단.
- **수정**: `streamlit_app/app.py`의 `DETECT_MAX` 640→1280.
- **팔레트 견인(몸 전체 색 전환)은 손대지 않음** — 의도적 보류. 이유: ① 토큰
  예산이 이미 빠듯함(과거 실측 최악 75/77, SD1.5 시절 94토큰 초과로 꼬리 유실
  회귀 전례) — 검증 없이 문구 추가 시 다른 부위가 잘릴 위험. ② **오늘 strength를
  0.85→0.7로 낮춘 것과 상충 가능성** — strength를 낮춰 원형 구조가 잘 살아남게
  했는데, 그 구조에는 원형의 원래 색도 포함됨 → 팔레트 견인이 오히려 더 약해졌을
  수 있음. strength와 팔레트 견인을 함께 그리드로 재검증해야 하는 문제라, GPU
  예산이 있는 별도 세션에서 다루는 게 맞음(오늘은 비용 초과로 보류).

### 진행 상황 7 (2026-08-15 밤) — 원형 풀 "선명도" 재검토 (49→45)
- **사용자 실사용 리포트**: 실제 결과물이 "구름처럼 몽실몽실, 형태가 제대로 안
  잡힘" — 디버그 패널(얼굴 크롭+매칭된 원형) 확인 결과 **얼굴 검출은 정상, 매칭된
  원형(`budgerigar.png`) 자체가 부위 경계가 흐린 개체**였음.
- **원인**: strength 0.7로 낮추면서 원형 구조가 더 많이 살아남게 됐는데(의도한
  효과), **원형 자체의 결함(흐린 선·부실한 채색)도 같이 살아남는 부작용**이 있음
  — strength 0.85 시절엔 LoRA가 덮어써서 흐린 원형도 어느 정도 정리돼 보였던 것.
  큐레이션 기준이 "부위가 있냐"까지만이고 "선이 선명하냐"는 안 봤던 것도 원인.
  ⚠ 이건 오늘 낮춘 strength 값 자체가 잘못됐다는 뜻이 아니라, 원형 풀 큐레이션
  기준이 하나 더 필요했다는 뜻(strength 0.7은 여전히 유효, 되돌리지 않음).
- **49종 전체 선명도 재검토**(GPU 불필요, 정지 이미지 육안 확인): **4종 제외**
  — `budgerigar`(사용자가 지적한 그 원형, 선/채색 부실) · `bear_cub`(부위 경계
  없는 감자 덩어리형) · `deer_fawn`(동물 2마리 동시 등장 — 이중 피사체) ·
  `hamster`(동물 2마리 겹침 — 이중 피사체). 나머지 45종은 선·부위 경계 크리스프
  확인. **최종 원형 도감 = 45종**.
- **후속 과제**: proto_dex_batch.py 큐레이션 기준에 "선명도"·"단일 피사체" 체크를
  명시적으로 추가하면 다음 확장(11종 재시도분 등) 때 같은 문제 재발 방지 가능
  — 이번엔 사후 검토로 처리, 사전 필터는 미구현.

### 다음 세션 재개 키워드 (2026-08-15)
`팔레트 견인(strength 0.7과 함께 재그리드)` `원형 도감 확장(11종 재시도 + 선명도 사전 필터)`

### 다음 세션 재개 키워드
`SDXL 전환` `애니 베이스 검증` `cutemon 재학습` `입·팔다리 디테일`

### 재개 시 작업 (사용자 합의됨)
1. **1단계 — SDXL 검증 프로브** (~30분·학습 없음): SDXL 애니 특화 베이스(Illustrious-XL /
   Animagine XL 4 / Pony V6 중 HF 가용 확인) + 공개 포켓몬 스타일 SDXL LoRA +
   `h94/IP-Adapter-FaceID`의 SDXL판으로 현 파이프라인 재현 → 현 SD1.5 결과와 그리드 비교.
   근거: 남은 품질 격차(입 뭉개짐·팔다리 소실·과변형 붕괴)가 SD1.5 디테일 한계에 걸림.
2. **2단계 — 통과 시 cutemon LoRA SDXL 재학습** (~4~6시간, 밤에 걸어두기 추천):
   `dataset/full` + `tools/build_*` 도구 그대로, diffusers `train_text_to_image_lora_sdxl.py`.
3. 실패/보류 시 SD1.5 경로 유지(secrets 롤백 가능) — 현 상태도 "합격선" 품질.

### 이전 상태 (2026-08-14 오전) — SD1.5 최종 (SDXL 전환 전, 커밋됨)
- **파이프라인**: 얼굴 → CLIP 특징 점수판(13후보 중 1개) → **구조형 체형 필터**(무사지 원형
  배제) + 상위 5 중 시드 선택 원형 → img2img(변신 0.85) + 체형별 사지 어휘 + 속성 팔레트
  → 도감 카드. LoRA = v3(1025장) 1.1 + v2(큐레이션 200장) 0.7 혼합.
- **사용자 평가**: "포켓몬같은 원형, 키메라 없음" 기준 합격선. 남은 격차 = 입 선명도·팔다리
  표현·특징(점 등) 가시성 → SDXL 전환의 목적.
- 실행: `cd streamlit_app && .venv\Scripts\python -m streamlit run app.py`
  (첫 요청 시 모델 로드 ~3분, 원형 인덱싱 ~20초)

### 이전 상태 (2026-08-13 심야 2차) — 레퍼런스 합성 모드 + v3 LoRA

### 현재 아키텍처 (사용자 설계: 특징 1개 → 원형 1마리 → 합성)
- **레퍼런스 합성**: CLIP 특징 점수판(13개 후보, 마진 최고 1개) + 사람 얼굴과 CLIP 유사도
  top-1 원형(큐레이션 200장 인덱스) → **img2img(변신 0.85)** 합성. 단일 원형 밑그림이라
  키메라 구조적 차단. ControlNet·rembg·실루엣 밴드 경로 제거(파이프라인 단순화).
- **v3 LoRA 배포**: 전체 1025장(흰배경 합성 + 색 캡션, `tools/build_full_dataset.py`) ×
  5000스텝 학습, **checkpoint-4000** 채택 — v2 대비 포켓몬 비례(둥근 몸+귀 지느러미+짧은
  팔다리)와 단일 종 일관성이 명확히 우세(probe_v3 그리드).
- **단순 눈**: "simple small dot eyes"+"flat colors" + 네거티브 realistic eyes 계열 —
  사람 눈처럼 그려지던 문제 해소(probe8). 변신 정도 기본 0.85(0.7은 원형과 너무 유사).
- ⚠ IP: 특정 원형 앵커 + 포켓몬 어휘 학습 — 실서비스 전 클린 데이터 재학습 필수(§10).
- 학습 이력: v1(큐레이션 200×2000스텝) → v2(200×3500) → **v3(1025×5000, 배포)** 전부 로컬 $0.

### 이전 상태 (2026-08-13 심야) — 원형 모드 + v2 LoRA + 속성·도감 카드

### 최종 아키텍처 (사용자 피드백 2회 반영)
- **원형 모드**: "모든 특징 이식" → "포켓몬 원형 + 대표 특징 1개"로 전환(사용자 지시).
  ControlNet 입력 = **실루엣 밴드**(경계 13px 링, 얼굴 내부 선 제거) + **CLIP 제로샷 특징 1개**
  (안경/수염/긴머리/뻗친머리, 마진 미달 시 순수 원형)를 프롬프트로 주입.
  §6.2에서 배제했던 특징 텍스트화는 목표가 정체성→원형으로 바뀌며 정답이 됨.
- **v2 LoRA 채택**: 3500스텝 학습 후 **checkpoint-3000** 배포. v1 대비 공식 아트워크풍
  평면 셀 셰이딩·또렷한 외곽선·팔레트 적중(풀→초록, 불→주황+불꽃 갈기) 확인.
- **속성 시스템(§12-8 계층 시드 완성)**: 사진(임베딩)→시드·실루엣, 이름 해시→속성
  (불/물/풀/전기/페어리)→몸 색 팔레트. **도감 카드**(속성색 테두리·뱃지·도감 No.) PIL 합성.
- 검증: 시드 6개 강건성(probe4) · 원형 모드(probe5) · v1 vs v2(probe_v2) 그리드 실측
  + e2e 스모크("아쿠아"→물 타입 파란 크리처+안경+카드).

### 이전 상태 (2026-08-13 저녁) — 모듈형 전환 완료

### 완료 — 인계 작업 1·2·3 전부
- **모듈형 스택 전환**: `app.py` = 표준 SD1.5 + 스타일 LoRA + IP-Adapter FaceID + lineart ControlNet.
  베이스·LoRA 모두 secrets 교체 지점(`BASE_MODEL`, `STYLE_LORA`) — 자체 학습 LoRA 완성 시 STYLE_LORA만 교체, 구 모놀리식 롤백은 `BASE_MODEL=lambdalabs/sd-pokemon-diffusers` + `STYLE_LORA=""`.
- **가중치 균형 (probe2 실측 답)**: 크리처화의 결정 변수는 LoRA 가중치가 아니라
  ① **트리거 어휘** — 프롬프트에 학습 캡션 지배 패턴("a drawing of a ... pokemon")이 없으면
  같은 가중치에서도 카툰화된 사람이 나옴. ② **ControlNet 완화** — 0.65는 사람 구조 잔존,
  **0.5 = 균형점**, 0.35는 크리처지만 닮음 소실. LoRA는 1.0이면 충분(1.2와 차이 없음).
  기본값: CN 0.5 · LoRA 1.0 · FaceID 0.45.
- **계층 시드**: 얼굴 임베딩 SHA-256(주) + 이름(보조) → 같은 사진·같은 이름 = 같은 몬스터.
  결정론·이름 변형 모두 스모크 테스트로 검증(같은 입력 = 동일 PNG 바이트).
  한계: 같은 인물의 **다른 사진**은 임베딩이 달라 시드가 다름 — 인물 단위 고정은 임베딩 저장소가 필요한 Phase 2 과제(FaceID의 정체성 견인으로 부분 보완).

### 자체 LoRA v1 학습 완료 (2026-08-13 밤)
- **구성**: SD1.5 · rank 16 · 2000스텝(200장×40에폭) · fp16+grad ckpt · 로컬 RTX 4060에서 52분 · $0.
  트리거 `cutemon`, **색 보존 캡션**(`tools/build_captions.py` — 이미지별 주요 색 2개 삽입).
- **vs 공개 LoRA(pcuenq)**: 닮음·색 보존(갈색 머리 유지)은 v1 압승, 크리처화는 기본 가중치에서 부족
  → probe3로 해소: **L1.3 × FaceID 0.35 × CN 0.4**에서 헤어스타일이 귀·색 패치로 은유되는 크리처 확보.
- **앱 적용 완료**: `models/style_lora/`(gitignore, 재현은 `training/lora_v1` 또는 재학습),
  프롬프트 트리거 cutemon 교체("pokemon" 어휘 제거 — §10 정합), 기본값 C0.4/L1.3/F0.35.
- 학습 스크립트: diffusers v0.39.0 `train_text_to_image_lora.py`(`training/`, gitignore).

### 다음 작업
1. **실기기 e2e + 지인 5명 테스트** (§11 Phase 1 기준: a+b 3/5).
2. **A/B 비교표** (§11 Phase 2 증빙) — 구 모놀리식 vs 모듈형 경로.
3. (품질 미달 시) v2 학습: 3000스텝+ / rank 32 / 캡션 어휘 확장.

### 함정 기록 (재발 방지)
- 2023년식 LoRA(attn-proc 포맷)는 `load_lora_weights`가 **에러 없이 조용히 무시**한다
  → `app.py::_style_lora_state()`가 PEFT 키로 변환. 자체 학습본(모던 포맷)은 그대로 통과.
- FaceID 체크포인트는 `faceid_0`이라는 내장 LoRA를 등록한다 —
  `set_adapters(["style"])`만 호출하면 FaceID가 꺼짐. 반드시 둘 다 활성화.
- 이 노트북(RAM 16GB)에서 SD 파이프라인 프로세스 **동시 2개 금지** — 스왑으로 로딩이 수십 분대로 늘어짐.

### 현재 스택
| 역할 | 모델 |
|---|---|
| 생성 베이스 | `stable-diffusion-v1-5/stable-diffusion-v1-5` |
| 스타일 | **자체 v1 LoRA** `streamlit_app/models/style_lora` @ 1.3 (트리거 cutemon, 폴백 pcuenq) |
| 정체성 | `h94/IP-Adapter-FaceID` @ 0.35 + insightface `buffalo_l` 임베딩 |
| 형태 제어 | `lllyasviel/control_v11p_sd15_lineart` @ 0.4 |
| 윤곽선 | `lllyasviel/Annotators` LineartDetector |
| 얼굴 검출 | YuNet `models/yunet.onnx` (박스) — insightface(임베딩)와 용도 분리 |
| 배경 제거 | rembg `u2net` + 턱 아래 컷(0.80) |
| 시드 | 얼굴 임베딩 SHA-256 + 이름 해시 |

### 이전 상태 (2026-08-13 오전) — 중단 시점 인계

#### 직전 검증 결과
**표준 SD1.5 + lineart ControlNet + IP-Adapter FaceID** (스타일 LoRA 없이) 실측:
- ✅ **FaceID가 표준 베이스에서 강력히 작동.** 원본 인물과 명확히 닮은 얼굴, 안경까지 정확 반영.
  기존 `sd-pokemon-diffusers` 베이스에서 효과가 미미했던 원인이 "풀 파인튜닝된 베이스가
  어댑터를 막는다"였음이 확인됨.
- ❌ 결과가 **"카툰화된 사람"** — 크리처가 아님. 네거티브 `human, person`이 FaceID에 압도됨.
- → **닮음은 해결 가능. 남은 과제는 스타일로 사람→크리처 방향으로 밀어내기.**

### 다음 작업 (순서대로)
1. **스타일 LoRA 확보** — 자체 학습(`dataset/cute` 200장 준비 완료) 또는 공개 LoRA로 선검증
   - 자체 학습이 PRD §2 차별점("직접 학습 증빙")의 핵심이므로 최종적으로는 자체 학습 필요
   - `runwayml/stable-diffusion-v1-5`는 HF에서 삭제됨 → `stable-diffusion-v1-5/stable-diffusion-v1-5` 사용
2. **가중치 균형 탐색** — LoRA weight 0.6~0.8 × FaceID scale 0.3~0.6
   - FaceID가 세면 사람이 되고, 약하면 안 닮음. 이 축이 최종 품질을 결정함
3. **시드 결정론** (§12-8 계층 시드) — 현재 같은 사람도 매번 다른 몬스터가 나옴.
   얼굴 임베딩 → 해시 → 시드로 "같은 사람 = 같은 몬스터" 확보. insightface 설치 완료돼 코드 10줄
4. **A/B 비교** — 기존 pokemon-diffusers 경로 vs 새 모듈형 경로. `BASE_MODEL`이 secrets로
   교체 가능하므로 롤백 가능

### 완료된 것 (커밋 순)
- `8a013db` git init, Gemini 버전 보존. `.gitignore` 실키 노출 버그 수정(`**/` 누락)
- `bea7f77` 로컬 파이프라인 전환 (API 결제 벽 회피, 생성 5초/장)
- `2faa8cb` 얼굴 검출(YuNet) + EXIF 회전 보정 — 배경이 특징으로 섞이던 문제
- `bd82f94` EXIF 없는 사진 대비 90도 회전 재시도
- `781a69f` 프롬프트에서 `monster` 제거 — 악타입 소환의 원인 (BLIP 캡션 편향)
- `8ef4ad7` 큐레이션 도구 (PokéAPI 1025장 → CLIP 점수 → 상위 200장)
- `d114c08` ControlNet softedge → lineart, 기본 강도 0.65
- `e9ce0e9` 배경 제거 (rembg u2net)
- `6914c9d` 턱 아래 컷 — 수건/옷 질감 제거

### 현재 스택
| 역할 | 모델 |
|---|---|
| 생성 베이스 | `lambdalabs/sd-pokemon-diffusers` (교체 예정) |
| 형태 제어 | `lllyasviel/control_v11p_sd15_lineart` @ 0.65 |
| 윤곽선 | `lllyasviel/Annotators` LineartDetector |
| 얼굴 검출 | YuNet `models/yunet.onnx` |
| 배경 제거 | rembg `u2net` + 턱 아래 컷(0.80) |
| 미사용(설치됨) | `h94/IP-Adapter-FaceID`, insightface `buffalo_l`, CLIP |

### 미해결 이슈
- **정체성 일관성 없음** — 같은 사람도 시드에 따라 전혀 다른 몬스터 (→ 작업 3번)
- **귀여움 부족** — 베이스 모델이 2022년 SD1.4 파인튜닝이라 한계 (→ 작업 1번)
- **Streamlit Cloud 배포 불가** — 무료 티어에 GPU 없음. §8 Phase 1 산출물 수정 필요

### 실행 방법
```
cd C:\Mark42\monsnap\streamlit_app
.venv\Scripts\python -m streamlit run app.py
```

### 이전 상태 (2026-08-12) — 로컬 생성으로 전환

**결정: 상용 API 포기, 로컬 GPU 생성으로 Phase 1 진행.** 근거:
- Gemini 이미지 모델은 무료 티어가 **구조적으로 0**(신형 `gemini-3.1-flash-image`까지 전부 `limit: 0`, 2026-08-12 실측). 모델 교체로 우회 불가.
- 2026-03 정책 개편으로 신규 계정은 **선불 의무**(후불은 Tier 3만), 최소 충전 ₩25,000·환불 불가. GCP $300 크레딧은 "AI Studio Gemini API 비용에 사용 불가"로 명시 제외.
- 대안 API 조사 결과 fal.ai(FLUX Kontext dev $0.015/장, Nano Banana 재판매 엔드포인트 보유)가 최선이었으나, **팀에 SD1.5 + LoRA + ControlNet 경험이 이미 있어** 로컬 $0 경로의 실현 가능성이 API 결제보다 높다고 판단.

**채택 스택** (팀 보유 기술 3/4 재활용):

| 부품 | 선택 | 팀 경험 |
|---|---|---|
| 베이스 | SD1.5 파생 (`lambdalabs/sd-pokemon-diffusers`) | ✅ |
| 스타일 | UNet LoRA (AdamW) — 우선 공개 LoRA로 검증 후 자체 학습 | ✅ |
| 형태 유지 | ControlNet softedge (`control_v11p_sd15_softedge`) | ✅ (scribble) |
| 정체성 | IP-Adapter FaceID — **닮음 미달 시에만 추가** | ❌ |

**§12-4 해소**: 마스코트형↔크리처형은 기획 결정이 아니라 `controlnet_conditioning_scale` 파라미터(0.3 크리처형 ~ 0.9 마스코트형). UI 슬라이더로 노출해 스윕 가능.

- **완료**: `git init` + 초기 커밋(8a013db, Gemini 버전 보존). `.gitignore` 버그 수정 — `.streamlit/secrets.toml` 패턴이 슬래시 때문에 루트에만 적용돼 `streamlit_app/.streamlit/secrets.toml`(실키)이 커밋될 뻔함 → `**/` 로 수정. `app.py`를 로컬 diffusers 파이프라인으로 재작성. PyTorch 2.6.0+cu124 설치, CUDA 인식 확인(RTX 4060 Laptop 8GB, 드라이버 528.92로도 마이너 버전 호환 통과).
- **검증 중**: 모델 다운로드(~5GB) + 생성 스모크 테스트 + 스케일 스윕.
- **다음**: ① 실사진으로 닮음 검증(§11 기준: 5명 중 3명) ② 미달 시 IP-Adapter FaceID 추가 ③ 통과 시 자체 LoRA 학습으로 공개 LoRA 교체 ④ Streamlit Cloud 배포는 **불가**(무료 티어 GPU 없음) — 데모는 로컬 시연/녹화로 대체, §8 Phase 1 산출물 수정 필요.
- **키 상태**: Gemini 실키는 `streamlit_app/.streamlit/secrets.toml`에 남아 있으나 현재 코드 경로에서 미사용(gitignore 확인됨).

### 이전 상태 (2026-08-10)

- **완료**: `streamlit_app/app.py` 작성 — 카메라/앨범 선택 UI, FR-1(파일 검증)·FR-3(결과 표시·PNG 다운로드·재생성)·FR-4(에러 3종: 파일 오류/API 오류/안전필터 차단) 구현. `streamlit_app/.venv`에 의존성 설치 완료.
- **검증 완료**: Streamlit UI 정상 로드(브라우저로 확인). Gemini API 호출 코드 경로 정상 — SDK 호출이 구글 서버까지 도달해 구조화된 응답을 받음(모델명·프롬프트·`Part.from_bytes` 사용법 전부 정상). 재현 스크립트는 세션 임시 파일이라 소실됨 — 필요 시 `app.py`의 `generate()` 함수 로직 그대로 재작성하면 됨.
- **블로킹 (미해결)**: 이 Gemini 프로젝트의 이미지 생성 모델(`gemini-2.5-flash-preview-image`)은 무료 할당량이 **구조적으로 0**(요청 수·토큰 수 전부, 재시도해도 동일 — 일시 소진 아님). §12-3 오픈 이슈의 실측 답. **결제(billing) 활성화 필요**: https://aistudio.google.com/apikey 에서 프로젝트 업그레이드, 또는 Google Cloud Console(console.cloud.google.com/billing)에서 결제 계정 연결.
- **키 상태**: 실키는 `streamlit_app/.streamlit/secrets.toml`에 있음(`.gitignore` 등록됨). `.example` 파일은 플레이스홀더로 정상 복구됨. 저장소는 아직 `git init` 전.
- **다음 액션**: ① 결제 활성화(사용자) ② `cd streamlit_app && .venv\Scripts\python -m streamlit run app.py`로 재기동 ③ 실물 사진으로 브라우저 e2e 재검증 ④ 정상 생성 확인되면 §11 성공지표(지인 5명 테스트) 진행 ⑤ GitHub 레포 생성·Streamlit Community Cloud 배포는 미착수.
